# Chatroom

This is a chatroom implemented in python using sockets for IPC, and select (systemcall) for handling several
connections (clients) in the chatroom (I/O multiplexing).

This is a TUI based program.
- To send a message, you should press Ctrl+g.
- To exit, you should write "exit".
